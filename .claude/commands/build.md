---
description: Build the full .NET solution
---

Run `dotnet build BikeHausFreiburg.sln` from the repo root. Report any errors or warnings. If the build succeeds, summarize in one line ("Build succeeded, N warnings"). Do not run tests; this is build-only.
