---
description: Run the BikeHaus API in development mode
---

Run `dotnet run --project BikeHaus.API` in the background. The API listens on http://localhost:5196. Watch initial output for "Now listening on" and report the URL. If startup fails (port in use, migration error), report the exact error verbatim. Do not block waiting — start it in the background and move on.
